const array = [1, 'a', 2, null, 3, undefined, 4, {}, 5, 6, 'word', 7, 8, false, 9, 100]
function filter(num) {
    for (let i = 0; num > 0; i++)
        return num < 100
}
console.log(array.filter(filter))